toDO:
    visual:
        - Add all progress live ans visible to the html so user see that stuff is happening.
    
    code:
        - add session Storage and cache aufrufe
        - need to call the main function after username checks out